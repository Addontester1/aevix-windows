<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Aevix_App.Pages.OnboardingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="{ThemeResource AevixCoral}" Offset="0" />
                <GradientStop Color="{ThemeResource AevixTeal}"  Offset="1" />
            </LinearGradientBrush>
        </Grid.Background>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="16" MaxWidth="560">
            <TextBlock Text="Aevix" FontSize="64" FontWeight="SemiBold" Foreground="White" HorizontalAlignment="Center" />
            <TextBlock Text="Native Windows IPTV — fast, focused, free of fluff."
                       Foreground="White" Style="{StaticResource SubtitleTextBlockStyle}" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" />
            <TextBlock Text="Add a playlist to begin. Supports M3U URLs, M3U files, Xtream Codes, and Stalker portals."
                       Foreground="#CCFFFFFF" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" />
            <Button Content="Add your first playlist" Click="Add_Click" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Center" Margin="0,16,0,0" />
            <Button Content="Skip for now" Click="Skip_Click" HorizontalAlignment="Center" />
        </StackPanel>
    </Grid>
</Page>
